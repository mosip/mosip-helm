{{- if .Values.databases.mosip_kernel.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: db-kernel-secrets
  namespace: {{ .Release.Namespace }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: postgres
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  su-password: {{ .Values.databases.mosip_kernel.suPassword | b64enc | quote }}
  sysadmin-password: {{ .Values.databases.mosip_kernel.sysadminPassword | b64enc | quote }}
  dbadmin-password: {{ .Values.databases.mosip_kernel.dbadminPassword | b64enc | quote }}
  appadmin-password: {{ .Values.databases.mosip_kernel.appadminPassword | b64enc | quote }}
  dbuser-password: {{ .Values.databases.mosip_kernel.dbuserPassword | b64enc | quote}}
{{- end }}
