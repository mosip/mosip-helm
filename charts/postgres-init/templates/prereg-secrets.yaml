{{- if .Values.databases.mosip_prereg.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: db-prereg-secrets
  namespace: {{ .Release.Namespace }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: postgres
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  su-password: {{ .Values.databases.mosip_prereg.suPassword | b64enc | quote }}
  sysadmin-password: {{ .Values.databases.mosip_prereg.sysadminPassword | b64enc | quote }}
  dbadmin-password: {{ .Values.databases.mosip_prereg.dbadminPassword | b64enc | quote }}
  appadmin-password: {{ .Values.databases.mosip_prereg.appadminPassword | b64enc | quote }}
  dbuser-password: {{ .Values.databases.mosip_prereg.dbuserPassword | b64enc | quote}}
{{- end }}
