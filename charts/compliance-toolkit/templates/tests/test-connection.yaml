apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mosip-compliance-toolkit.fullname" . }}-test-connection"
  labels:
    {{- include "mosip-compliance-toolkit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mosip-compliance-toolkit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
