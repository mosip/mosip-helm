apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compliance-toolkit.fullname" . }}-test-connection"
  labels:
    {{- include "compliance-toolkit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "compliance-toolkit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
